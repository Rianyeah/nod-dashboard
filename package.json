{
  "dependencies": {
    "@playwright/test": "^1.60.0"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  }
}
